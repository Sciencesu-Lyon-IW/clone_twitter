{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <div>
        <div>
            <header>
                    <ul>
                        <li><a href="{{ path('home') }}">Home</a></li>
                        <li><a href="{{ path('home') }}">Explore</a></li>
                        <li><a href="{{ path('home') }}">Notifications</a></li>
                        <li><a href="{{ path('home') }}">Messages</a></li>
                        <li><a href="{{ path('home') }}">Bookmarks</a></li>
                        <li><a href="{{ path('home') }}">Lists</a></li>
                        <li><a href="{{ path('user_profile') }}">Profile</a></li>
                        <li><a href="{{ path('home') }}">More</a></li>
                    </ul>
                <button>Tweet</button>
                <div>
                    <div>Username</div>
                </div>
            </header>
        </div>
    </div>
{% endblock %}
